{
  "mcpServers": {
    "axme": {
      "command": "node",
      "args": [
        "${CLAUDE_PLUGIN_ROOT}/server.mjs"
      ],
      "env": {
        "NODE_PATH": "${CLAUDE_PLUGIN_DATA}/node_modules"
      }
    }
  }
}
